//change this interface to a static class of extension methods
public static class DictionaryListMethods
{
    // static async string dictionaryListAsString(this Dictionary<TKey, TValue>)
    // {
    //     foreach (var key in Dictionary<TKey, TValue))
    //     {

    //     } 
    // }
    // ^ this code doesn't work for some reason

    static int count;
    public static string GetDictionaryListFor()
    {
        return null;
    }
    public static void AddItemToDictionaryList(object item, object thisObject)
    {

    }
}